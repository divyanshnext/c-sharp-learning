class Run_Time_Intialization
{
    public static void Run()
    {
        // run time intialization // value will be taken from user // and then assigned to variable
        int a = Convert.ToInt32(Console.ReadLine());
        Console.WriteLine("The value of a is :"+a);
    }
}